import { CopyToClipboardDirective } from './copy.directive';

describe('CopyToClipboardDirective', () => {
  it('should create an instance', () => {
    const directive = new CopyToClipboardDirective();
    expect(directive).toBeTruthy();
  });
});
